namespace MediaTransCoder.Backend {
    /// <summary>
    /// Message type and priority, used to determine interface behavior
    /// </summary>
    public enum MessageType {
        INFO,
        WARNING,
        ERROR,
        SUCCESS,
        DEBUG
    }
}
