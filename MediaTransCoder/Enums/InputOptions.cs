namespace MediaTransCoder.Backend {
    /// <summary>
    /// Input option - if file one file is specified or the program will look for supported files in provided directory
    /// </summary>
    public enum InputOptions {
        FILE,
        DIRECTORY,
        RECURSIVE
    }
}
