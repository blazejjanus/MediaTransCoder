namespace MediaTransCoder.Backend {
    /// <summary>
    /// Preset purpose - smaller files, better quality, faster conversion
    /// </summary>
    public enum PresetTarget {
        SIZE,
        QUALITY,
        SPEED
    }
}
